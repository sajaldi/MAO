services:
  web:
    build: .
    ports:
      - "7070:7070"
    environment:
      - DEBUG=0
      - SECRET_KEY=your-secret-key-change-in-production
      - ALLOWED_HOSTS=*
    volumes:
      - ./data:/app/data
    restart: unless-stopped
